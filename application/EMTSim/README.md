# EMTSim

Draft application specification placeholder.

This document will define the EMTSim command-line application, usage, and
solver input format after the design interview. It is intentionally minimal
for now so the first committed shape reflects explicit design choices rather
than assumptions.
